<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqupt.software_9.mapper.CategoryMapper">
    <insert id="savetest" parameterType="com.cqupt.software_9.entity.CategoryEntity">
        INSERT INTO category (id, cat_level, label, parent_id, is_leafs, is_common, path, is_delete, is_wide_table)
        VALUES (#{id}, #{catLevel}, #{label}, #{parentId}, #{isLeafs}, #{isCommon}, #{path}, #{isDelete}, #{isWideTable})
    </insert>

    <select id="getall" resultType="com.cqupt.software_9.entity.CategoryEntity">
        SELECT * FROM category
    </select>


    <delete id="removeNode">
        DELETE FROM category WHERE id = #{id}
    </delete>
    <select id="countDisease" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM category WHERE is_leafs = 0
    </select>
    <select id="countTable" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM category WHERE is_leafs = 1
    </select>
    <select id="isRepeatCategory" resultType="java.lang.String">
        SELECT label FROM category WHERE label = #{label}
    </select>


    <select id="getLevel2Label" resultType="com.cqupt.software_9.entity.CategoryEntity">
        SELECT * FROM category WHERE cat_level=2;
    </select>

    <select id="getLabelsByPid" resultType="com.cqupt.software_9.entity.CategoryEntity">
        SELECT * FROM category WHERE parent_id = #{pid};
    </select>

    <update id="updateTableNameByTableId">
        update category set label=#{tableName}, status=#{tableStatus} where id = CAST(#{tableid} AS VARCHAR);;
    </update>
</mapper>