<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cqupt.software_9.mapper.DetailMapper">
    <delete id="deleteByModelname">
        DELETE FROM detail
        <where>
            <if test="modelname != null and modelname != ''">
                AND modelname LIKE CONCAT(#{modelname}, '%')
            </if>
        </where>
    </delete>


    <select id="getDetail" resultType="com.cqupt.software_9.common.DetailDTO">
        SELECT feature, importance,fpercentage,fvalue,bpercentage,bvalue FROM detail WHERE modelname  = #{modelname}
    </select>
    <select id="getAll" resultType="com.cqupt.software_9.common.DetailDTO">
        SELECT feature, importance,fpercentage,fvalue,bpercentage,bvalue FROM detail WHERE modelname  = #{modelname}
    </select>
</mapper>

